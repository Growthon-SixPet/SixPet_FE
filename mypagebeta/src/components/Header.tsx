export default function Header() {
  return (
    <header
      style={{
        height: 64,
        background: "#ffffff",
        borderBottom: "1px solid #e5e7eb",
      }}
    >
      <div
        style={{
          width: 1280,
          margin: "0 auto",
          height: "100%",
          display: "flex",
          alignItems: "center",
          fontWeight: 800,
        }}
      >
        Header (임시)
      </div>
    </header>
  );
}
